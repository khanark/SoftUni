function storeCityInfo(name, population, treasury) {
	// create city collection
	const cityInfo = { name, population, treasury };

	// return the city collection
	return cityInfo;
}
storeCityInfo('Tortuga', 7000, 15000);
